if [ -f /etc/xcalibrate.conf ]; then
	if [ -x /usr/bin/xtscal ]; then
		xtscal -cal `cat /etc/xcalibrate.conf`
	else
		if [ -x /usr/X11R6/bin/xcalibrate ]; then
			/usr/X11R6/bin/xcalibrate -cal `cat /etc/xcalibrate.conf`
		fi
	fi
else
	sh /usr/bin/gpe-xcalibrate.sh
fi
