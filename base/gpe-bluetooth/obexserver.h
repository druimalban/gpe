extern void import_vcard (const gchar *data, size_t len);
extern void import_vcal (const gchar *data, size_t len);
extern void import_unknown (const char *name, const gchar *data, size_t len);

extern int obex_init (void);

