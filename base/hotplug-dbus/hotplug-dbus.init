#!/bin/sh

case "$1" in
	start)
		echo "Setting up D-BUS hotplug"
		echo "/sbin/hotplug-dbus" >/proc/sys/kernel/hotplug
		;;
	stop)
		echo "Stopping D-BUS hotplug"
		echo "/sbin/hotplug" >/proc/sys/kernel/hotplug
		;;
esac
