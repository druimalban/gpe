desktopdir = $(datadir)/applications

if HILDON
desktop_DATA =
else
desktop_DATA = starling.desktop
endif

pixmapdir = $(datadir)/pixmaps
pixmap_DATA = bass.png

EXTRA_DIST = $(desktop_DATA).in bass.png
